import { createState, AdjustHook, injectCSS, injectHTML, injectTitle } from './MintUtils.js';

export const Mint = {
    createState,
    AdjustHook,
    injectCSS,
    injectHTML,
    injectTitle,
};